DATABASE_PATH=./data/app.db
STORAGE_PATH=./storage
API_HOST=0.0.0.0
API_PORT=3001
VITE_API_URL=http://localhost:3001
WORKER_POLL_MS=1500
EMBEDDING_PROVIDER=local
# Optional. Downloads the model on first use when set to clip.
CLIP_MODEL=Xenova/clip-vit-base-patch32
